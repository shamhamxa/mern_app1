generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model auth {
  id       Int     @id @default(0)
  username String?
  password String?
}

model data {
  id     BigInt  @id @default(autoincrement())
  title  String?
  detail String?
}
