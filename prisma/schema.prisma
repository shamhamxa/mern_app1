generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model auth {
  id       Int     @id @default(autoincrement())
  username String
  password String
}

model data {
  id     Int  @id @default(autoincrement())
  title  String?
  detail String?
}
