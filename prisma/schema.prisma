generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model auth {
  id       BigInt  @id @default(autoincrement())
  username String?
  password String?
}

model data {
  id     BigInt  @id @default(autoincrement())
  title  String?
  detail String?
}
